{
  "name": "workerd-issue-6583",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run --max-workers=1 --no-isolate",
    "test:sqlite": "vitest run --max-workers=1 --no-isolate src/node-sqlite.test.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.6",
    "@types/bun": "latest",
    "vitest": "^4.1.4",
    "wrangler": "^4.82.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
